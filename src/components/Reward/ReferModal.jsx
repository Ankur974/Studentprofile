import React from "react";
import styled from "styled-components";

import Modal from "../common/ui/Modal";
import FlexBox from "../common/ui/FlexBox";

const Wrapper = styled(FlexBox)``;

const Refer = () => {
  return (
    <Modal M1>
      <Wrapper>dummy</Wrapper>
    </Modal>
  );
};

export default Refer;
